// This module was previously a visual currency icon. We keep a lightweight
// export to avoid breaking any stray imports: it just exposes the literal
// 'PKR' string for use in UI where needed.

const PKR = "PKR";
export default PKR;
